node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
full_backup.zip

# uploads folder — ignore all files, keep the folder via .gitkeep
uploads/*
!uploads/.gitkeep

*.zip
*.tar.gz
backup/

